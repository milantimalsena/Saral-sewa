database:
  type: psql
  plan: free

services:
  - type: web
    name: saral-sewa
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn --chdir backend saralsewa.wsgi:application
    pythonVersion: "3.11"
